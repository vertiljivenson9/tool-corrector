{
  "name": "tool-corrector",
  "version": "1.0.0",
  "description": "Local-first secure code editing tool for function replacement",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages publish dist"
  },
  "keywords": [
    "code-editor",
    "local-first",
    "function-replacement",
    "web-tool"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
