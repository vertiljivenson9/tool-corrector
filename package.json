{
  "name": "tool-corrector",
  "version": "1.0.0",
  "description": "Local-first secure code editing tool for function replacement",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "wrangler pages deploy dist --project-name=tool-corrector"
  },
  "keywords": [
    "code-editor",
    "local-first",
    "function-replacement",
    "web-tool"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  }
}
